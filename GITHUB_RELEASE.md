# Инструкция по публикации релизов на GitHub Release

Это руководство поможет вам опубликовать новую версию ChatList на GitHub Release.

## Подготовка к релизу

### Шаг 1: Обновите версию

1. Откройте файл `version.py`
2. Обновите версию:
   ```python
   __version__ = "1.1.0"  # Новая версия
   ```
3. Сохраните файл

### Шаг 2: Соберите установщик

```powershell
# Убедитесь, что вы в корне проекта
cd c:\Work\ChatList

# Соберите исполняемый файл
.\build.ps1

# Создайте установщик
.\build-installer.ps1
```

Установщик будет создан в папке `installer\ChatList-Setup-{версия}.exe`

### Шаг 3: Подготовьте файлы для релиза

Создайте папку `release` и скопируйте туда файлы:

```powershell
# Создайте папку release (если её нет)
New-Item -ItemType Directory -Path "release" -Force

# Скопируйте установщик
Copy-Item "installer\ChatList-Setup-*.exe" -Destination "release\" -Force

# (Опционально) Скопируйте исполняемый файл для портативной версии
Copy-Item "dist\ChatList.exe" -Destination "release\ChatList-Portable.exe" -Force
```

## Создание релиза на GitHub

### Способ 1: Через веб-интерфейс GitHub

1. **Перейдите на страницу релизов:**
   - Откройте ваш репозиторий на GitHub
   - Нажмите на "Releases" в правой панели
   - Или перейдите по адресу: `https://github.com/progtips/ChatList/releases`

2. **Создайте новый релиз:**
   - Нажмите "Draft a new release" или "Create a new release"
   - Или нажмите "Choose a tag" и создайте новый тег

3. **Заполните информацию о релизе:**
   - **Tag version:** `v1.0.0` (используйте формат `v{версия}`)
   - **Release title:** `ChatList v1.0.0` или просто `v1.0.0`
   - **Description:** Используйте шаблон из `.github/release-template.md` или см. пример ниже

4. **Загрузите файлы:**
   - Перетащите файл `ChatList-Setup-{версия}.exe` в область "Attach binaries"
   - (Опционально) Загрузите портативную версию `ChatList-Portable.exe`

5. **Опубликуйте релиз:**
   - Нажмите "Publish release"

### Способ 2: Через GitHub CLI (gh)

Если у вас установлен GitHub CLI:

```powershell
# Авторизуйтесь (если еще не авторизованы)
gh auth login

# Создайте тег
git tag -a v1.0.0 -m "Release v1.0.0"

# Отправьте тег на GitHub
git push origin v1.0.0

# Создайте релиз с файлом
gh release create v1.0.0 `
  "release\ChatList-Setup-1.0.0.exe" `
  --title "ChatList v1.0.0" `
  --notes-file ".github\release-template.md"
```

### Способ 3: Через Git команды

```powershell
# Создайте тег
git tag -a v1.0.0 -m "Release v1.0.0"

# Отправьте тег на GitHub
git push origin v1.0.0

# Затем создайте релиз через веб-интерфейс GitHub, выбрав созданный тег
```

## Шаблон описания релиза

Используйте следующий шаблон для описания релиза:

```markdown
## Что нового в версии {версия}

### ✨ Новые возможности
- Описание новой функции 1
- Описание новой функции 2

### 🐛 Исправления ошибок
- Исправлена ошибка с...
- Улучшена производительность...

### 🔧 Технические улучшения
- Обновлены зависимости
- Улучшена стабильность

### 📦 Установка

**Установщик Windows:**
- Скачайте `ChatList-Setup-{версия}.exe`
- Запустите установщик и следуйте инструкциям

**Портативная версия:**
- Скачайте `ChatList-Portable.exe`
- Запустите файл напрямую (установка не требуется)

### 📝 Полный список изменений

См. [CHANGELOG.md](CHANGELOG.md) для полного списка изменений.

### ⚠️ Известные проблемы

- Если обнаружены проблемы, укажите их здесь

### 🙏 Благодарности

Спасибо всем, кто помог с разработкой и тестированием!
```

## Автоматизация через GitHub Actions

Для автоматической публикации релизов создайте файл `.github/workflows/release.yml` (см. отдельный файл).

## Рекомендации

1. **Версионирование:** Используйте [Semantic Versioning](https://semver.org/):
   - `MAJOR.MINOR.PATCH` (например, `1.0.0`)
   - MAJOR - несовместимые изменения API
   - MINOR - новая функциональность с обратной совместимостью
   - PATCH - исправления ошибок

2. **Теги:** Всегда создавайте теги в формате `v{версия}` (например, `v1.0.0`)

3. **Описание:** Всегда заполняйте описание релиза - это помогает пользователям понять, что изменилось

4. **Тестирование:** Перед публикацией обязательно протестируйте установщик на чистой системе

5. **Архивы:** GitHub автоматически создает архивы для каждого релиза

## Проверка после публикации

После публикации релиза проверьте:

1. ✅ Файл установщика загружен и доступен для скачивания
2. ✅ Описание релиза отображается корректно
3. ✅ Тег создан и связан с релизом
4. ✅ Страница релиза доступна по адресу: `https://github.com/progtips/ChatList/releases/tag/v{версия}`

## Откат релиза

Если нужно откатить релиз:

1. Перейдите на страницу релиза
2. Нажмите "Edit release"
3. Выберите "Unpublish release" (если релиз опубликован)
4. Или удалите тег: `git push origin --delete v{версия}`

